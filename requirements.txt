# No LLM SDK required — no AWS Bedrock, no Anthropic, no OpenAI.
# GitHub Copilot acts on GitHub Issues natively inside GitHub.

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# GitHub integration (create issues, read manifests)
PyGithub>=2.4.0

# HTTP client (JFrog Xray REST API + PyPI version lookup)
httpx>=0.27.0

# Config via environment variables
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Manifest parsing (pyproject.toml / Pipfile)
tomlkit>=0.13.0

# Version comparison and resolution
packaging>=24.0

typing_extensions>=4.12.0
